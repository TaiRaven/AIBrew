---
phase: 03-recipe-presets
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified: []
autonomous: false
requirements:
  - RECIPE-02

must_haves:
  truths:
    - "Active presets appear in the list; archived presets do not"
    - "Preset card shows correct name, method chip, and computed ratio"
    - "New Preset modal saves and card appears without reload"
    - "Editing a preset field persists after re-open"
    - "Archive removes preset from list; no archived tab visible"
    - "Non-admin aibrew_user account can CRUD presets"
    - "User without aibrew_user role cannot access recipe records"
  artifacts:
    - path: ".planning/phases/03-recipe-presets/03-04-SUMMARY.md"
      provides: "Phase 3 UAT sign-off record"
      contains: "APPROVED"
  key_links:
    - from: "non-admin test user"
      to: "x_664529_aibrew_recipe table"
      via: "ACL enforcement on instance"
      pattern: "aibrew_user role"
---

<objective>
Human UAT checkpoint: verify all Phase 3 success criteria on the deployed instance using a non-admin `aibrew_user` account. The non-admin test is mandatory per CLAUDE.md Critical Pitfalls — ACL gaps are invisible to admin.

Purpose: Confirm RECIPE-02 is fully satisfied before marking Phase 3 complete.

Output: Human sign-off or list of issues to fix.
</objective>

<execution_context>
@/Users/William.Wood/.claude/get-shit-done/workflows/execute-plan.md
@/Users/William.Wood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\William.Wood\AIBrew\.planning\ROADMAP.md
@C:\Users\William.Wood\AIBrew\.planning\phases\03-recipe-presets\03-CONTEXT.md
@C:\Users\William.Wood\AIBrew\.planning\phases\03-recipe-presets\03-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT — Verify all Phase 3 criteria on instance</name>
  <what-built>
    - x_664529_aibrew_recipe table with all 8 columns, deployed to instance
    - 4 ACLs protecting the table for aibrew_user role
    - RecipeSection.tsx: list view (card grid), create modal, detail/edit view, archive flow
    - CatalogView.tsx: Recipes tab enabled
  </what-built>
  <how-to-verify>
    Open the AIBrew app on the ServiceNow instance. Log in as a NON-ADMIN user who has the `aibrew_user` role.

    **Test 1 — List view and empty state:**
    1. Navigate to Catalog > Recipes tab
    2. Confirm the tab is clickable (not greyed out)
    3. If no presets exist: confirm the dashed placeholder box shows with "+" character and "No presets yet" / "Create one to pre-fill the brew form"

    **Test 2 — Create a preset:**
    1. Tap "+ New Preset" button
    2. Confirm modal opens with "New Preset" heading
    3. Fill in: Name = "V60 Morning", Method = Pour Over, Dose = 18, Water = 300, Grind size = 22
    4. Tap Save (FormActionBar)
    5. Confirm modal closes and a new card appears in the list WITHOUT a page reload
    6. Confirm card shows: "V60 Morning" (large bold text) + "Pour Over" method chip (colored background) + "18g • 300g  1:16.7" ratio line

    **Test 3 — Ratio computation:**
    1. Create a second preset with Dose = 20, Water = 320
    2. Confirm card shows ratio "1:16.0" (320/20 = 16.0)
    3. Create a preset with Dose = 0 (leave blank)
    4. Confirm ratio shows "—" (not NaN or crash)

    **Test 4 — Edit a preset:**
    1. Tap the "V60 Morning" card
    2. Confirm detail view opens with all fields editable via FormColumnLayout
    3. Confirm "← Recipes" back link is visible
    4. Confirm "Archive" button is visible (top-right area)
    5. Change the Name to "V60 Morning Adjusted" and save
    6. Navigate back to list, re-open the preset
    7. Confirm the name change persisted

    **Test 5 — Archive a preset:**
    1. Open a preset in detail view
    2. Tap "Archive"
    3. Confirm confirmation modal opens with text "Archive this preset?" and "Archived presets won't appear in the brew form or this list."
    4. Tap "Archive" in the modal footer
    5. Confirm navigation returns to the Recipes list
    6. Confirm the archived preset NO LONGER appears in the list
    7. Confirm there is NO "Archived" tab visible anywhere

    **Test 6 — ACL enforcement (non-admin CRUD):**
    - All tests above run as non-admin aibrew_user: should work without errors
    - Optionally: log in as a user WITHOUT the aibrew_user role and confirm the recipe records are not accessible

    **Test 7 — Mobile layout (if testable):**
    1. Resize browser to ≤ 400px width or use mobile device
    2. Confirm preset cards display in a single column (not 2 columns)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all tests pass.
    If any tests fail, describe the issue (e.g., "ratio shows NaN", "Archive modal doesn't close") — Claude will fix and re-deploy before the next attempt.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-admin user → instance | ACL enforcement test — verifies server-side role check is active |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Elevation of Privilege | ACL gaps on x_664529_aibrew_recipe | mitigate | This UAT checkpoint IS the mitigation: non-admin user must successfully CRUD; user without role must be blocked; test before phase sign-off (CLAUDE.md Critical Pitfalls requirement) |
</threat_model>

<verification>
Human sign-off: "approved" response confirms all 7 UAT test cases passed.
</verification>

<success_criteria>
- All 7 UAT test groups pass with a non-admin aibrew_user account
- Human types "approved"
- Phase 3 success criteria are met:
  1. (DEFERRED) Save-from-brew — Phase 4
  2. User can view all saved presets in the dedicated management screen — VERIFIED
  3. User can edit a preset's name and values, and archive a preset, from that screen — VERIFIED
</success_criteria>

<output>
After approval, create `.planning/phases/03-recipe-presets/03-04-SUMMARY.md` with UAT results and mark Phase 3 complete in STATE.md.
</output>
