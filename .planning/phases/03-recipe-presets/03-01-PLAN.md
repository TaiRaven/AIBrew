---
phase: 03-recipe-presets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fluent/tables/recipe.now.ts
  - src/fluent/acls/recipe-acls.now.ts
  - src/fluent/index.now.ts
autonomous: true
requirements:
  - RECIPE-02
  - RECIPE-01  # DEFERRED to Phase 4 per D-07 — schema built here is the contract Phase 4 depends on

must_haves:
  truths:
    - "The x_664529_aibrew_recipe table exists on the instance with all 8 columns"
    - "Only users with the aibrew_user role can read, create, write, or delete recipe records"
    - "The recipe table exposes `name` as the display column so RecordProvider shows preset name not sysId"
    - "User can enter fractional gram values (e.g. 18.5g) in dose and water fields without error"
    - "A newly created preset immediately appears in the active list without manual activation"
  artifacts:
    - path: "src/fluent/tables/recipe.now.ts"
      provides: "x_664529_aibrew_recipe table definition"
      contains: "DecimalColumn"
    - path: "src/fluent/acls/recipe-acls.now.ts"
      provides: "4 record-level ACLs for recipe table"
      exports: ["recipe_read", "recipe_write", "recipe_create", "recipe_delete"]
    - path: "src/fluent/index.now.ts"
      provides: "export registration for recipe artifacts"
      contains: "x_664529_aibrew_recipe"
  key_links:
    - from: "src/fluent/tables/recipe.now.ts"
      to: "src/fluent/tables/equipment.now.ts"
      via: "ReferenceColumn import"
      pattern: "x_664529_aibrew_equipment"
    - from: "src/fluent/acls/recipe-acls.now.ts"
      to: "src/fluent/roles/aibrew-user.now.ts"
      via: "role import"
      pattern: "aibrew_user"
    - from: "src/fluent/index.now.ts"
      to: "src/fluent/tables/recipe.now.ts"
      via: "export"
      pattern: "export.*recipe"
---

<objective>
Create the Fluent build-time artifacts for the recipe preset feature: the `x_664529_aibrew_recipe` table definition and its four record-level ACLs. Register both in `index.now.ts` and deploy to the instance so the table exists when the UI component is built in Wave 2.

Purpose: The recipe table is the permanent schema contract that Phase 4 (brew log preset picker) depends on. Field names must match exactly — they cannot be renamed after deployment without data migration.

Output: `recipe.now.ts`, `recipe-acls.now.ts`, updated `index.now.ts`, deployed to instance.
</objective>

<execution_context>
@/Users/William.Wood/.claude/get-shit-done/workflows/execute-plan.md
@/Users/William.Wood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\William.Wood\AIBrew\.planning\ROADMAP.md
@C:\Users\William.Wood\AIBrew\.planning\phases\03-recipe-presets\03-CONTEXT.md
@C:\Users\William.Wood\AIBrew\.planning\phases\03-recipe-presets\03-RESEARCH.md

<interfaces>
<!-- Canonical patterns the executor must replicate exactly. -->

From src/fluent/tables/bean.now.ts (exact pattern to follow):
```typescript
import '@servicenow/sdk/global'
import { Table, StringColumn, BooleanColumn, ChoiceColumn, ReferenceColumn, DateColumn } from '@servicenow/sdk/core'
import { x_664529_aibrew_roaster } from './roaster.now'

export const x_664529_aibrew_bean = Table({
  name: 'x_664529_aibrew_bean',
  label: 'Bean',
  display: 'name',
  schema: {
    name:        StringColumn({ label: 'Name', mandatory: true, maxLength: 100 }),
    // ... other columns
    active:      BooleanColumn({ label: 'Active', default: true }),
  },
})
```

From src/fluent/acls/bean-acls.now.ts (exact pattern to follow):
```typescript
import '@servicenow/sdk/global'
import { Acl } from '@servicenow/sdk/core'
import { aibrew_user } from '../roles/aibrew-user.now'

export const bean_read = Acl({
  $id: Now.ID['bean_read'],
  type: 'record',
  table: 'x_664529_aibrew_bean',
  operation: 'read',
  roles: [aibrew_user],
})
export const bean_write = Acl({ $id: Now.ID['bean_write'], type: 'record', table: 'x_664529_aibrew_bean', operation: 'write', roles: [aibrew_user] })
export const bean_create = Acl({ $id: Now.ID['bean_create'], type: 'record', table: 'x_664529_aibrew_bean', operation: 'create', roles: [aibrew_user] })
export const bean_delete = Acl({ $id: Now.ID['bean_delete'], type: 'record', table: 'x_664529_aibrew_bean', operation: 'delete', roles: [aibrew_user] })
```

From src/fluent/index.now.ts (current state — append recipe exports after line 14):
```typescript
export { x_664529_aibrew_roaster } from './tables/roaster.now'
export { x_664529_aibrew_equipment } from './tables/equipment.now'
export { aibrew_user } from './roles/aibrew-user.now'
export { roaster_read, roaster_write, roaster_create, roaster_delete } from './acls/roaster-acls.now'
export { equipment_read, equipment_write, equipment_create, equipment_delete } from './acls/equipment-acls.now'
export { aibrew_menu, aibrew_home_module } from './navigator/aibrew-menu.now'
export { aibrew_home } from './ui-pages/aibrew-home.now'
export { x_664529_aibrew_bean } from './tables/bean.now'
export { x_664529_aibrew_bean_purchase } from './tables/bean-purchase.now'
export { bean_read, bean_write, bean_create, bean_delete } from './acls/bean-acls.now'
export { bean_purchase_read, bean_purchase_write, bean_purchase_create, bean_purchase_delete } from './acls/bean-purchase-acls.now'
export { bean_stock_api } from './scripted-rest/bean-stock-api.now'
export { BeanStockHelper } from './script-includes/bean-stock-helper.now'
// Add recipe exports after this line
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create recipe.now.ts table definition</name>
  <files>src/fluent/tables/recipe.now.ts</files>
  <read_first>
    - src/fluent/tables/bean.now.ts — canonical Table() pattern; copy structure exactly
    - src/fluent/tables/equipment.now.ts — confirm export name x_664529_aibrew_equipment for ReferenceColumn import
  </read_first>
  <action>
Create `src/fluent/tables/recipe.now.ts` as a NEW file with the following exact content. Do not deviate from field names — they are the Phase 4 contract:

```typescript
import '@servicenow/sdk/global'
import {
  Table, StringColumn, BooleanColumn, ChoiceColumn,
  ReferenceColumn, IntegerColumn, DecimalColumn, MultiLineTextColumn,
} from '@servicenow/sdk/core'
import { x_664529_aibrew_equipment } from './equipment.now'

export const x_664529_aibrew_recipe = Table({
  name: 'x_664529_aibrew_recipe',
  label: 'Recipe Preset',
  display: 'name',
  schema: {
    name:           StringColumn({ label: 'Name', mandatory: true, maxLength: 100 }),
    method:         ChoiceColumn({
                      label: 'Method',
                      choices: {
                        pour_over:    { label: 'Pour Over' },
                        espresso:     { label: 'Espresso' },
                        french_press: { label: 'French Press' },
                        aeropress:    { label: 'AeroPress' },
                        moka_pot:     { label: 'Moka Pot' },
                        cold_brew:    { label: 'Cold Brew' },
                        other:        { label: 'Other' },
                      },
                    }),
    equipment:      ReferenceColumn({
                      label: 'Equipment',
                      referenceTable: x_664529_aibrew_equipment.name,
                    }),
    dose_weight_g:  DecimalColumn({ label: 'Dose (g)' }),
    water_weight_g: DecimalColumn({ label: 'Water (g)' }),
    grind_size:     IntegerColumn({ label: 'Grind Size' }),
    notes:          MultiLineTextColumn({ label: 'Notes', maxLength: 1000 }),
    active:         BooleanColumn({ label: 'Active', default: true }),
  },
})
```

Field names are FIXED — `name`, `method`, `equipment`, `dose_weight_g`, `water_weight_g`, `grind_size`, `notes`, `active`. Do NOT rename, abbreviate, or add a `ratio` column (ratio is computed at render time per D-03). Do NOT add a `bean` reference (bean-agnostic per D-01).
  </action>
  <verify>
    <automated>npx @servicenow/sdk build</automated>
  </verify>
  <acceptance_criteria>
    - src/fluent/tables/recipe.now.ts exists
    - File contains `DecimalColumn` (two new column types for this project)
    - File contains `MultiLineTextColumn`
    - File contains `display: 'name'`
    - File contains `x_664529_aibrew_recipe`
    - File contains `referenceTable: x_664529_aibrew_equipment.name`
    - File does NOT contain `ratio` (ratio is never stored)
    - File does NOT contain a `bean` column (bean-agnostic per D-01)
    - `npx @servicenow/sdk build` exits 0
  </acceptance_criteria>
  <done>recipe.now.ts compiles without error; all 8 schema fields present with correct types</done>
</task>

<task type="auto">
  <name>Task 2: Create recipe-acls.now.ts and update index.now.ts</name>
  <files>src/fluent/acls/recipe-acls.now.ts, src/fluent/index.now.ts</files>
  <read_first>
    - src/fluent/acls/bean-acls.now.ts — exact 4-ACL pattern to replicate; copy $id naming convention
    - src/fluent/index.now.ts — current export list; append recipe exports at the bottom
  </read_first>
  <action>
**Step A — Create `src/fluent/acls/recipe-acls.now.ts`:**

```typescript
import '@servicenow/sdk/global'
import { Acl } from '@servicenow/sdk/core'
import { aibrew_user } from '../roles/aibrew-user.now'

export const recipe_read = Acl({
  $id: Now.ID['recipe_read'],
  type: 'record',
  table: 'x_664529_aibrew_recipe',
  operation: 'read',
  roles: [aibrew_user],
})

export const recipe_write = Acl({
  $id: Now.ID['recipe_write'],
  type: 'record',
  table: 'x_664529_aibrew_recipe',
  operation: 'write',
  roles: [aibrew_user],
})

export const recipe_create = Acl({
  $id: Now.ID['recipe_create'],
  type: 'record',
  table: 'x_664529_aibrew_recipe',
  operation: 'create',
  roles: [aibrew_user],
})

export const recipe_delete = Acl({
  $id: Now.ID['recipe_delete'],
  type: 'record',
  table: 'x_664529_aibrew_recipe',
  operation: 'delete',
  roles: [aibrew_user],
})
```

**Step B — Append to `src/fluent/index.now.ts`** (add two lines after the last existing export):

```typescript
export { x_664529_aibrew_recipe } from './tables/recipe.now'
export { recipe_read, recipe_write, recipe_create, recipe_delete } from './acls/recipe-acls.now'
```

**Step C — Build and deploy:**

```bash
npx @servicenow/sdk build && npx @servicenow/sdk install
```
  </action>
  <verify>
    <automated>npx @servicenow/sdk build && npx @servicenow/sdk install</automated>
  </verify>
  <acceptance_criteria>
    - src/fluent/acls/recipe-acls.now.ts exists and exports recipe_read, recipe_write, recipe_create, recipe_delete
    - Each ACL has `type: 'record'`, `table: 'x_664529_aibrew_recipe'`, and `roles: [aibrew_user]`
    - src/fluent/index.now.ts contains `export { x_664529_aibrew_recipe } from './tables/recipe.now'`
    - src/fluent/index.now.ts contains `export { recipe_read, recipe_write, recipe_create, recipe_delete } from './acls/recipe-acls.now'`
    - `npx @servicenow/sdk build` exits 0
    - `npx @servicenow/sdk install` exits 0 (table created on instance)
  </acceptance_criteria>
  <done>Four ACLs deployed; x_664529_aibrew_recipe table visible in ServiceNow Studio on the instance</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK build → instance | Fluent artifacts pushed via `sdk install`; SDK handles authentication and scoping |
| aibrew_user role → recipe table | Only users with the role can perform record operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | x_664529_aibrew_recipe table (all operations) | mitigate | Four Acl() definitions cover read/write/create/delete — only `aibrew_user` role granted; no `admin`-only bypass; verified with non-admin test in Plan 04 |
| T-03-02 | Tampering | recipe-acls.now.ts | accept | SDK-managed ACLs are replaced on every `sdk install` — manual on-instance edits are overwritten, preventing drift |
</threat_model>

<verification>
- `npx @servicenow/sdk build` passes (compile gate)
- `npx @servicenow/sdk install` passes (deploy gate)
- `src/fluent/tables/recipe.now.ts` exists with DecimalColumn, MultiLineTextColumn, display: 'name'
- `src/fluent/acls/recipe-acls.now.ts` exports 4 ACLs targeting x_664529_aibrew_recipe
- `src/fluent/index.now.ts` exports both recipe table and ACLs
- No `ratio` column in schema (computed at render only)
- No `bean` column in schema (bean-agnostic per D-01)
</verification>

<success_criteria>
- x_664529_aibrew_recipe table exists on the instance with all 8 columns (name, method, equipment, dose_weight_g, water_weight_g, grind_size, notes, active)
- Four record-level ACLs are active for aibrew_user role
- SDK build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-recipe-presets/03-01-SUMMARY.md`
</output>
