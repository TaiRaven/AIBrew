---
phase: 02-bean-catalog-inventory
plan: 05
type: execute
wave: 3
depends_on:
  - 02-03-PLAN.md
files_modified:
  - src/client/components/CatalogView.tsx
autonomous: false
requirements:
  - CAT-04
  - CAT-05
  - CAT-06
  - INV-01
  - INV-02
  - INV-03
  - INV-04

must_haves:
  truths:
    - "The 'Beans' tab in CatalogView is enabled (not disabled) and renders BeanSection"
    - "SDK build exits 0 after CatalogView change"
    - "App deploys to instance successfully via npx @servicenow/sdk install"
    - "User can navigate to Catalog > Beans and see the bean list"
    - "All 5 phase success criteria verified by human UAT with non-admin test user"
  artifacts:
    - path: "src/client/components/CatalogView.tsx"
      provides: "Beans tab enabled, BeanSection imported and wired in renderSection()"
  key_links:
    - from: "CatalogView.tsx SUB_NAV"
      to: "beans entry"
      via: "disabled: false"
      pattern: "beans.*disabled.*false"
    - from: "CatalogView.tsx renderSection()"
      to: "BeanSection component"
      via: "case 'beans': return <BeanSection params={params} />"
      pattern: "case 'beans'"
---

<objective>
Wire BeanSection into CatalogView (enable the Beans tab, import BeanSection, add the switch case), deploy the full Wave 1+2+3 build to the instance, and run human UAT to verify all 5 phase success criteria.

Purpose: This is the integration and delivery step. Without this plan the Beans tab remains disabled and all Phase 2 work is invisible to the user. UAT with a non-admin test user confirms ACLs work and all 7 requirements are satisfied.

Output: CatalogView.tsx updated, full app deployed, all 5 success criteria signed off via human UAT.
</objective>

<execution_context>
@C:/Users/William.Wood/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/William.Wood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current CatalogView.tsx structure — 3 precise changes needed, nothing else -->

From src/client/components/CatalogView.tsx (current structure — executor MUST read before modifying):

Change 1 — SUB_NAV beans entry (currently disabled: true):
```typescript
// Before:
{ id: 'beans', label: 'Beans', disabled: true },
// After:
{ id: 'beans', label: 'Beans', disabled: false },
```

Change 2 — Import (add after last existing import):
```typescript
import BeanSection from './BeanSection'
```

Change 3 — renderSection() switch (add case before default):
```typescript
case 'beans':
  return <BeanSection params={params} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire BeanSection into CatalogView and build</name>
  <files>src/client/components/CatalogView.tsx</files>
  <read_first>
    - src/client/components/CatalogView.tsx — full current file (identify exact line numbers for 3 changes)
    - src/client/components/BeanSection.tsx — confirm default export name is BeanSection
  </read_first>
  <action>
Make exactly 3 changes to `src/client/components/CatalogView.tsx`:

1. Find the SUB_NAV array entry for beans (currently `disabled: true`). Change to `disabled: false`. Do NOT change any other SUB_NAV entry.

2. Add `import BeanSection from './BeanSection'` in the imports block, after the existing `import EquipmentSection` line (or at end of component imports block — keep alphabetical/logical order consistent with existing imports).

3. In `renderSection()` (or whatever switch/if function dispatches to section components), add:
   ```typescript
   case 'beans':
     return <BeanSection params={params} />
   ```
   Place this case before the `default:` case. Match the indent and style of existing cases exactly.

No other changes to CatalogView.tsx. Do not modify TopNav, HomeView, or any other file.

After editing, run:
```bash
cd C:/Users/William.Wood/AIBrew
npx @servicenow/sdk build
```

Build must exit 0 before proceeding to the deploy task.
  </action>
  <verify>
    <automated>cd C:/Users/William.Wood/AIBrew && npx @servicenow/sdk build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/client/components/CatalogView.tsx` contains `import BeanSection from './BeanSection'`
    - `src/client/components/CatalogView.tsx` contains `disabled: false` on the beans SUB_NAV entry (grep shows `beans.*disabled.*false` or `disabled.*false` on a line near `beans`)
    - `src/client/components/CatalogView.tsx` contains `case 'beans':`
    - `src/client/components/CatalogView.tsx` contains `BeanSection params={params}` (or `BeanSection params`)
    - `src/client/components/CatalogView.tsx` does NOT contain `disabled: true` for the beans entry
    - `npx @servicenow/sdk build` exits 0
  </acceptance_criteria>
  <done>CatalogView wired. Beans tab is active. Build passes. Ready for deploy.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to instance</name>
  <files></files>
  <read_first>
    - .planning/STATE.md — confirm instance URL and scope prefix before install
  </read_first>
  <action>
Deploy the full Phase 2 build to the instance:

```bash
cd C:/Users/William.Wood/AIBrew
npx @servicenow/sdk build && npx @servicenow/sdk install
```

If `install` prompts for credentials, use the stored auth from `npx now-sdk auth` (set up in Phase 1). If auth fails, the user must re-authenticate — stop and surface the error message.

Do not skip `build` before `install` — always build first to catch compile errors before pushing.

The install command deploys all artifacts registered in `src/fluent/index.now.ts`, which after Plan 01–05 includes:
- `x_664529_aibrew_bean` table
- `x_664529_aibrew_bean_purchase` table
- `bean_read/write/create/delete` ACLs
- `bean_purchase_read/write/create/delete` ACLs
- `bean_stock_api` Scripted REST API
- `BeanStockHelper` Script Include
- Updated `aibrew_home` UiPage client bundle (now includes BeanSection)
  </action>
  <verify>
    <automated>cd C:/Users/William.Wood/AIBrew && npx @servicenow/sdk build && npx @servicenow/sdk install 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx @servicenow/sdk install` exits 0 with no error messages
    - Install output mentions deploying or updating `x_664529_aibrew_bean` (or equivalent SDK artifact confirmation)
  </acceptance_criteria>
  <done>App deployed to instance. All Phase 2 artifacts live on the instance. Ready for UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Full Phase 2 implementation deployed: bean table, bean_purchase table, ACLs, Scripted REST stock endpoint, BeanSection React component with list view (tabs, card grid, stock bars, low-stock badge, create modal, archive) and detail view (RecordProvider form, KPI strip, stock bar, Add Beans form, purchase history).
  </what-built>
  <how-to-verify>
Open the AIBrew app from the ServiceNow navigator and verify each criterion below. Test with BOTH your admin user AND the non-admin test user (to verify ACLs).

**Success Criterion 1 — Bean CRUD (CAT-04, CAT-05, CAT-06):**
1. Navigate to Catalog > Beans tab — confirm it is enabled (not greyed out)
2. Click "+ New bean" — confirm create modal opens with fields: Name, Origin, Roast Level, Roast Date, Roaster reference
3. Fill in a bean and save — confirm the bean appears in the "In pantry" list
4. Click the bean card — confirm detail page opens with RecordProvider form showing the fields you entered
5. Edit the bean name and save — confirm the change is reflected
6. From the detail page, click "Archive" and confirm the confirmation modal — confirm the bean disappears from both "In pantry" and "Empty bags" tabs

**Success Criterion 2 — Purchase logging (INV-01):**
7. Open a bean detail page
8. In the "Add Beans" form, enter grams = 250 and today's date — click "Add Beans"
9. Confirm the purchase history row appears below the form with the date and 250g
10. Try submitting grams = 0 — confirm error message "Enter a valid number of grams" appears (inline, not a popup)

**Success Criterion 3 — Live stock (INV-02):**
11. After adding 250g in step 8, confirm the stock bar shows 250g / 250g (100% fill)
12. Confirm the KPI "on shelf" box shows 250g
13. Add another 100g purchase — confirm stock updates to 350g / 350g without a page reload

**Success Criterion 4 — Low-stock badge (INV-03):**
14. Create a bean and add a purchase of 30g
15. Confirm "⚠ Low stock" badge appears on the card in the list view AND on the detail page
16. Confirm the stock bar fill color is destructive red (not orange) for this bean

**Success Criterion 5 — Purchase history (INV-04):**
17. On a bean with multiple purchases, confirm the Purchase history section lists all entries newest-first
18. Confirm each row shows a date and gram amount

**Non-admin ACL test:**
19. Log in as the non-admin test user (with `x_664529_aibrew_user` role)
20. Confirm the user can create, view, and archive beans
21. Confirm the user can add purchases and see stock figures
22. Log in as a user WITHOUT the aibrew_user role — confirm they cannot access the bean or bean_purchase tables (403 or empty response)
  </how-to-verify>
  <resume-signal>Type "approved" if all 22 checks pass. If any check fails, describe which criterion failed and what behavior you observed — a gap closure plan will be created.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → CatalogView | Section routing from URL param `section=beans` drives component selection |
| Non-admin user → Tables | ACLs must block users without aibrew_user role from accessing bean and bean_purchase data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-03 | Elevation of Privilege | `x_664529_aibrew_bean` and `x_664529_aibrew_bean_purchase` tables | mitigate | UAT step 22 explicitly tests that a user without the `aibrew_user` role receives 403/empty response. ACLs deployed in Plans 01–02. Human verify step confirms enforcement. |
| T-2-02 | Elevation of Privilege | Scripted REST stock endpoint | mitigate | UAT test of stock endpoint with unauthenticated request should return 401. `authentication: true, authorization: true` set in Plan 02. |
</threat_model>

<verification>
Human UAT — all 22 checks in the checkpoint task must pass before this plan is marked complete.

Automated pre-deploy check:
```bash
cd C:/Users/William.Wood/AIBrew
npx @servicenow/sdk build && npx @servicenow/sdk install
```
</verification>

<success_criteria>
- Catalog > Beans tab is enabled and renders the bean list
- All 5 phase success criteria verified by the developer
- ACLs confirmed working for non-admin test user
- No console errors on the bean list or detail page
- `npx @servicenow/sdk install` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-bean-catalog-inventory/02-05-SUMMARY.md`
</output>
