---
phase: 06-analytics
plan: 03
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified: []
autonomous: false
requirements:
  - RPT-02
  - RPT-03
  - RPT-04

must_haves:
  truths:
    - "User can navigate to Analytics tab from TopNav and see all three chart sections"
    - "Rating Trend chart renders a polyline with dots for rated brews (or placeholder when < 2 exist)"
    - "Avg by Bean section shows horizontal bars ranked highest avg first"
    - "Avg by Method section shows horizontal bars ranked highest avg first"
    - "Unrated brews do not appear in any chart"
    - "Non-admin aibrew_user account can access the analytics view without ACL errors"
    - "Analytics tile on HomeView navigates correctly to analytics tab"
  artifacts:
    - path: "src/fluent/scripted-rest/analytics-api.now.ts"
      provides: "Registered on instance"
      contains: "analytics_api"
    - path: "src/client/components/AnalyticsView.tsx"
      provides: "Deployed to instance"
      contains: "AnalyticsView"
  key_links:
    - from: "browser (aibrew_user)"
      to: "/api/x_664529_aibrew/analytics/summary"
      via: "AnalyticsView.tsx fetch with X-UserToken g_ck"
      pattern: "analytics/summary"
---

<objective>
Deploy Plans 01 and 02 to the ServiceNow instance, then conduct full UAT with the non-admin aibrew_user account to verify all three analytics sections (RPT-02, RPT-03, RPT-04).

Purpose: Phase 5 UAT validated the non-admin check catches ACL gaps invisible to admin. This final plan ensures analytics is accessible to the actual app user and all three chart sections render correctly against real data.

Output: Deployed app + UAT sign-off checkpoint. Phase 6 complete.
</objective>

<execution_context>
@C:\Users\William.Wood\AIBrew\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\William.Wood\AIBrew\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\William.Wood\AIBrew\.planning\PROJECT.md
@C:\Users\William.Wood\AIBrew\.planning\ROADMAP.md
@C:\Users\William.Wood\AIBrew\.planning\STATE.md
@C:\Users\William.Wood\AIBrew\.planning\phases\06-analytics\06-01-SUMMARY.md
@C:\Users\William.Wood\AIBrew\.planning\phases\06-analytics\06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build and deploy</name>
  <files></files>
  <read_first>
    - .planning/phases/06-analytics/06-01-SUMMARY.md (confirm Plan 01 completed — backend deployed)
    - .planning/phases/06-analytics/06-02-SUMMARY.md (confirm Plan 02 completed — AnalyticsView.tsx and wiring changes delivered)
  </read_first>
  <action>
Verify Plan 01 and Plan 02 are both complete (check SUMMARYs exist). Then run a clean build and install:

```bash
npx @servicenow/sdk build
npx @servicenow/sdk install
```

If build fails: check TypeScript errors in the output. Common issues:
- AnalyticsView.tsx type errors on SVG props — use `as any` or correct SVG attribute names (e.g., `strokeWidth` not `stroke-width` in JSX)
- analytics-handler.ts import path errors — verify `../../server/analytics-handler` resolves from the scripted-rest subdirectory

If install fails after a successful build: check that the instance auth is still valid. Run `npx now-sdk auth --list` to confirm the instance is connected.

Both commands must exit with code 0 before proceeding to the UAT checkpoint.
  </action>
  <verify>
    <automated>npx @servicenow/sdk build</automated>
  </verify>
  <acceptance_criteria>
    - `npx @servicenow/sdk build` exits with code 0 and no TypeScript errors in output
    - `npx @servicenow/sdk install` exits with code 0
    - No "Module not found" or "Cannot find module" errors in build output
    - Output does not contain "error TS" (TypeScript compilation errors)
  </acceptance_criteria>
  <done>Clean build and successful install. All Phase 6 artifacts pushed to instance.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — Verify analytics with non-admin aibrew_user account</name>
  <what-built>
Plan 01 deployed a Scripted REST endpoint at GET /api/x_664529_aibrew/analytics/summary that aggregates rated brew data into trend, by_bean, and by_method arrays.

Plan 02 created AnalyticsView.tsx with a TrendChart SVG polyline and HorizontalBarChart div rows, and wired the Analytics tab + HomeView tile to navigate to the view.
  </what-built>
  <how-to-verify>
Log in to the ServiceNow instance as the non-admin `aibrew_user` account (not admin).

Open AIBrew from the application navigator.

**Test 1 — Navigation:**
- Verify the "Analytics" tab in the top nav is NOT greyed out (it was disabled before this phase)
- Click the Analytics tab — confirm the URL changes to `?view=analytics`
- Navigate back to Home — confirm the Analytics tile shows description "Your brew patterns"
- Click the Analytics tile — confirm it navigates to the analytics view

**Test 2 — Loading and data:**
- Reload the analytics page — confirm skeleton blocks appear briefly during fetch
- After load: confirm three sections appear with headings "Rating Trend", "Avg by Bean", "Avg by Method"
- If you have rated brews logged:
  - Rating Trend: confirm a polyline with dots appears; dots should be filled orange/red (`#c2410c`)
  - Avg by Bean: confirm bars appear, ordered highest avg first (top bar is highest rated bean)
  - Avg by Method: confirm bars appear, ordered highest avg first
  - Confirm each bar row shows: bean/method name (left), orange bar (center), avg score to 1 decimal (right)
  - Confirm each bar row shows a brew count subtitle (e.g. "3 brews") below the name

**Test 3 — Unrated brews exclusion:**
- If you have brews with no rating (or rating = 0), confirm they do NOT appear in the trend chart

**Test 4 — Sparse/empty states:**
- If you have fewer than 2 rated brews total: confirm the trend section shows "Rate at least 2 brews to see a trend line." (or "No rated brews yet..." if you have 0)
- If you have 0 rated brews for a specific bean or method: confirm that section shows its empty-state message rather than an empty chart

**Test 5 — ACL check:**
- Confirm no "Insufficient privileges" or ACL error appears in any section
- The analytics endpoint reads `x_664529_aibrew_brew_log` — confirm the aibrew_user role has read access (established in Phase 4 ACLs)

**If any test fails:** describe the failure in your resume signal.
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass. Or describe the specific failure (e.g., "Trend section shows ACL error", "Analytics tab still disabled", "Bars not sorted correctly") so a gap closure plan can be created.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Non-admin session → Analytics endpoint | UAT validates that aibrew_user (non-admin) can access the endpoint; ACL gap would be caught here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Elevation of Privilege | brew_log ACL for aibrew_user | mitigate | UAT Step 5 explicitly tests that aibrew_user can read brew_log data via the analytics endpoint. ACLs for brew_log_read were defined in Phase 4 Plan 01 — this plan verifies they extend to analytics reads. |
| T-06-08 | Information Disclosure | Admin vs non-admin data visibility | accept | Single-user app; no multi-user isolation required. All brew data belongs to the single aibrew_user. No PII or sensitive data exposure risk between users. |
</threat_model>

<verification>
UAT checkpoint in Task 2 covers all verification. All three RPT requirements must be verified by the non-admin user before the phase is considered complete.

Phase complete when:
- Task 1: build + install exit 0
- Task 2: human confirms "approved" for all five test scenarios

After UAT approval, update ROADMAP.md Phase 6 status to Complete and update STATE.md with completion date and lessons.
</verification>

<success_criteria>
- `npx @servicenow/sdk build && npx @servicenow/sdk install` exits 0
- Non-admin aibrew_user can open Analytics tab without ACL errors
- RPT-02: Rating trend chart renders scored brews over time (or placeholder if sparse)
- RPT-03: Avg by bean section shows horizontal bars ranked highest avg first
- RPT-04: Avg by method section shows horizontal bars ranked highest avg first
- No unrated brews appear in any section
- Analytics tile on HomeView navigates to analytics view
- Human UAT checkpoint returns "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/06-analytics/06-03-SUMMARY.md`

Also update:
- `.planning/ROADMAP.md`: Mark Phase 6 as Complete, update Plans count to 3
- `.planning/STATE.md`: Update progress (6/6 phases complete, 100%), record completion date, add Phase 6 lessons
</output>
